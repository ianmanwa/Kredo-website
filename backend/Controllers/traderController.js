const trader = (req,res)=>{
   
    //const { amount, direction } = req.body
    //if( !amount || !direction){
      //  res.json({message:"Please Fill in all the fields"})
    //}

    //GAME LOGIC GOES UNDER THIS



    //GAME LOGIC ENDS HERE

    res.json({message:"THE API WORKS"})
}

module.exports = {trader}